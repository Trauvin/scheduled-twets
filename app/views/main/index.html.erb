<div class="d-flex align-items-center justify-content-center">
  <h1>Welcome to Scheduled tweets!</h1>

  <% if @user %>
    Logeed in as: <%= @user.email %>
  <% end %> 

  <%= button_to "Logout", logout_path, method: :delete %>

</div>